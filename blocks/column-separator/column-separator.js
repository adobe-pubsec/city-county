/**
 * Purely a marker block — it exists so authors can indicate where a section
 * should split into columns (see data-columns handling in
 * blocks/section-metadata/section-metadata.js), which removes it once the
 * split has been made.
 */
export default function init() {}
